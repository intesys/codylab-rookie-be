server:
  port: 8080

spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:h2:mem:catalogogiv;DB_CLOSE_DELAY=-1
    username:
    password:

springdoc:
  swagger-ui:
    url: /v3/api-docs.yaml
