CREATE TABLE doctor (
    id bigint primary key,
    name varchar(128),
    surname varchar(128),
    phone_number varchar(32),
    address varchar(1024),
    email varchar(256),
    avatar varchar(128),
    profession varchar(128)
)
